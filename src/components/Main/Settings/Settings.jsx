import React from "react";
import main from "./Settings.module.css"

const Settings = (props) => {
    return (
        <div className={main}>
            Settings
        </div>
    );
}

export default Settings;