import React from "react";
import main from "./Profile.module.css"

const Profile = (props) => {
    return (
        <div className={main}>
            Profile
        </div>
    );
}

export default Profile;