import React from "react";
import main from "./Dialogs.module.css"

const Dialogs = (props) => {
    return (
        <div className={main}>
            Dialogs
        </div>
    );
}

export default Dialogs;